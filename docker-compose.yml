# Token Relay — single service for Zeabur
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - TZ=Asia/Shanghai
      - ADMIN_API_KEY=${ADMIN_API_KEY:-admin-master-key-change-me}
      - DB_PATH=/app/data/relay.db
